@media (min-width: 769px) {
  /* Solo logo del encabezado */
  header .logo {
    display: flex;
    align-items: center;
    justify-content: flex-start; /* Mueve el logo a la izquierda */
    margin-left: 20px; /* Ajusta la distancia según lo necesites */
  }

  /* Solo título del encabezado */
  header .main-title {
    font-size: 6rem !important; /* Mantiene el tamaño del título */
    text-align: left; /* Alinea el título a la izquierda */
    margin-left: 20px; /* Ajusta la posición del título */
  }
}