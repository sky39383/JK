@media (min-width: 769px) {
  /* Logo del encabezado */
  .logo {
    display: flex;
    align-items: center;
    justify-content: flex-start; /* Mueve el logo a la izquierda */
    margin-left: 0px; /* Ajusta la distancia según lo necesites */
  }

  .logo span {
    font-size: 4rem !important; /* Tamaño más grande del nombre, forzado */
    font-family: var(--brush-font);
    font-weight: bold;
  }

  /* Título del encabezado */
  .main-title {
    font-size: 6rem !important; /* Mantiene el tamaño del título */
    text-align: left; /* Mueve el título a la izquierda */
    margin-left: 20px; /* Ajusta la posición del título */
  }
}